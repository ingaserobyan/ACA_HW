#include <vector>
#include <list>

int main()
{
	std::vector<int> a;
	a.push_back(5);
	a.push_back(4);
	a.push_back(3);
	a.push_back(2);
	a.push_back(1);
	for(std::vector<int>::iterator itr = a.begin(); itr != a.end(); ++i)
	{

	}

	std::list<int>::iterator 

	return 0;
}
